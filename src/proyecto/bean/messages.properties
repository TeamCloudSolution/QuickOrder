LoginBean.getFormulariosPorMenu = select distinct f.* from formulario f, v_usuario_formulario v where f.estado = true and f.FORMULARIO_ID = v.formulario_id and f.menu_id = :menuId and v.LOGIN = :login order by f.nombre